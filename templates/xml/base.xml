<?xml version="1.0" encoding="UTF-8"?>{% load static %}<yml_catalog date="2020-02-26 09:00"><shop><name>Автопилот</name><url>https://avtopilot1.ru</url><currencies><currency id="RUR" rate="1"/></currencies><categories>{% for model in models %}<category id="{{ model.id }}">{{ model.make.title }} {{ model.title }}</category>{% endfor %}</categories><offers>{% for case in cases %}{% for material in case.children_materials.all %}{% if material.colors.exists %}{% for color in material.get_colors %}<offer id="{{ case.id }}"><name>{{ case.title }}, {{ color }}</name><url>https://avtopilot1.ru/case/{{ case.slug }}/?material={{ color.material_name|urlencode }}&amp;color={{ color.color_name|urlencode }}</url><price>{{ material.price }}</price><enable_auto_discounts>true</enable_auto_discounts><currencyId>RUR</currencyId><categoryId>{{ case.model_id }}</categoryId>{% if color.big_image %}<picture>{{ color.big_image.url }}</picture>{% elif case.image %}<picture>{{ case.image.url }}</picture>{% else %}<picture>{% static 'img/180x110.png' %}</picture>{% endif %}<delivery>true</delivery><pickup>true</pickup><description> <![CDATA[{{ case.description|safe }}]]> </description><manufacturer_warranty>true</manufacturer_warranty><param name="Марка автомобиля">{{ case.model.make.title }}</param><param name="Модель">{{ case.model.make.title }} {{ case.model.title }}</param><param name="Цвет">{{ color.color_name }}</param><param name="Материал">{{ color.material_name }}</param><param name="Комплект">да</param></offer>{% endfor %}{% endif %}{% endfor %}{% endfor %}</offers></shop></yml_catalog>